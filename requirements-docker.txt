# Runtime-only dependencies for the Docker environment server.
# Training deps (unsloth, vllm, triton, bitsandbytes) are NOT needed here.
# torch CPU-only is used for the environment; inference needs GPU separately.
fastapi>=0.110.0
uvicorn>=0.29.0
openai>=1.23.0
httpx>=0.27.0
tqdm>=4.66.0
python-dotenv>=1.0.0
ruff>=0.4.0
astor>=0.8.1
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers>=4.40.0
peft>=0.10.0
datasets
accelerate
huggingface_hub>=0.34.0
hf_transfer
sentencepiece>=0.2.0

